#!r6rs 

(import 
  (rnrs base (6))
  ;; composite standard library, imports most std libs
  (rnrs (6))
  ;; basic string formatting
  (srfi :28)
  (ray-tracing vector3D)
  (ray-tracing point3D)
  (ray-tracing ray3D))
